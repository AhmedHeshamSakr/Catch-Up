"""Neutral LLM layer: ADK runtime bridge and structured-output schemas.

This package sits below both ``app.pipeline`` and ``app.services`` so either
can import it without creating a dependency cycle between them.
"""
